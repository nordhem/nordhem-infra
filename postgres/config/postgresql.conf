# Nordhem Platform — PostgreSQL configuration
# Optimised for local development

# Connections
max_connections = 100

# Memory
shared_buffers = 256MB
work_mem = 16MB
maintenance_work_mem = 128MB

# Logging
log_statement = 'none'
log_min_duration_statement = 1000

# Timezone
timezone = 'UTC'
